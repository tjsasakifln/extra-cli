# QA Adversarial Review — cyc-2026-07-18T124230Z

**Story:** `ROI-cand-dyn-slice-1e78666e2a21`  
**Title:** [13.4 Qualidade mínima] Nenhum gate obrigatório usa `|| true`.  
**Reviewer:** adversarial-qa-auditor (Quinn) — **independent** of delivery-engineer  
**Branch:** `extra-roi/cand-no-or-true-gates`  
**Reviewed at:** 2026-07-18T12:47:50Z  

## Verdict: **CONCERNS**

DoD flip for the single item is **authorized** (`dod_flip_authorized: true`).  
Residual medium scanner defect + missing continuous CI wiring prevent a clean PASS.

---

## Commands re-run (independent)

| Command | Exit | Result |
|---------|------|--------|
| `python3 -m scripts.ops.scan_mandatory_gates_failclosed --json` | 0 | `ok=true`, scanned=7, findings=0, missing=0 |
| `python3 -m pytest tests/test_mandatory_gates_no_or_true.py -q -o addopts=` | 0 | **6 passed** |
| Manual line-scan of all `MANDATORY_GATE_PATHS` | 0 | Only ban-doc comments in `ci.yml` |
| Repo-wide non-comment `\|\| true` enumeration | 0 | 22 hits, all outside mandatory gate surface |

Evidence pack `docs/ops/session-2026-07-18-no-or-true-gates/` matches (scan.exit=0, pytest.exit=0, 6 passed).

---

## Falsification results

| Attempt | Outcome |
|---------|---------|
| Scanner greenwash via re-implemented tests? | **Fail to falsify** — tests import shipped `scan_repo` / `scan_text` / `main` |
| Mandatory path list too narrow? | **Partially** — list is curated/static; current out-of-list `\|\| true` are ops cleanup/notify, not CI gates |
| Hidden suppressors in ci.yml? | **Fail to falsify** — 3× continue-on-error + 1× `\|\| true` are **comments only** |
| DoD flipped without evidence? | **Fail to falsify** — item still `[ ]` in `DOD.md` |
| Scanner evasion via ban-marker on code line? | **Falsified scanner completeness** — `cmd \|\| true  # no \|\| true` → 0 findings |
| Scanner in CI continuous gate? | **Not present** — point-in-time proof only |

---

## Issues

1. **MEDIUM** — Ban-marker allowlist suppresses real `|| true` on the same line. Fix comment detection order.  
2. **LOW** — Static `MANDATORY_GATE_PATHS` can lag new CI scripts.  
3. **LOW** — Scanner not wired into CI/ci_gate.  
4. **LOW** — AIOX state still `Ready` / `qa_verdict=PENDING` while story says `InReview`.

---

## Allowed vs forbidden claims

**Allowed**
- Mandatory gate surface (7 paths) has zero non-comment `|| true` / `continue-on-error: true` as of this review.
- Executable proof exists (scanner + tests + pack).

**Forbidden**
- Full-repo free of `|| true`
- Scanner evasion-proof or continuously enforced in CI
- LOCAL_RESILIENCE_READY / PRE_VPS_FINAL_READY / VPS / 95% coverage
- Story Done without PO close

---

## DoD items

| Item | Flip authorized? |
|------|------------------|
| Nenhum gate obrigatório usa `|| true`. | **YES** |

---

## Next agents

1. **@po** — may flip DoD checkbox with evidence link; close story after accepting CONCERNS residuals as debt.  
2. **@dev** (optional follow-up) — harden `_is_comment_or_doc`; wire scanner into CI.  
3. **@devops** — publish only after PO close + state reconciliation.

---

*Independent adversarial QA — implementer self-QA is not accepted as gate.*
