# QA Adversarial Review — cyc-2026-07-18T131002Z

**Story:** `ROI-cand-dyn-slice-ac8b6e76a7b2`  
**Title:** [25. Verdade, linguagem e claims permitidos] empty DoD orphan cleanup  
**Reviewer:** adversarial-qa-auditor (Quinn) — **independent** of delivery-engineer  
**Branch:** `extra-roi/cand-empty-dod-orphan`  
**Reviewed at:** 2026-07-18T13:13:11Z  
**HEAD base:** `7f48288` (delivery still uncommitted)

## Verdict: **PASS**

Empty orphan checkbox at former `DOD.md:1401` (`dod:c70748809b8a`, text=`""`) was **deleted as invalid markup** — **not** flipped to `[x]`.  
`generate_dynamic_candidates` now skips blank item text so empty markup cannot re-enter ranking[0].

---

## Commands re-run (independent)

| Command | Exit | Result |
|---------|------|--------|
| Empty-checkbox regex on `DOD.md` (current vs HEAD) | 0 | **CURRENT=0**, **HEAD=1** at L1401 (`- [ ] `) |
| `python3 -m pytest -q -o addopts= --tb=short squads/extra-dod-roi/tests/test_generate_candidates_skip_empty.py` | 0 | **1 passed** |
| Adversarial harness (`''` / `None` / whitespace / real) | 0 | Only real text becomes a candidate |
| `git diff DOD.md` | 0 | Single delete `-- [ ] `; **zero** added `[x]` lines |

Evidence pack `docs/ops/session-2026-07-18-empty-dod-orphan/` matches (pytest.exit=0, MANIFEST documents delete + filter).

---

## Falsification results

| Attempt | Outcome |
|---------|---------|
| Empty checkbox still present? | **Fail to falsify cleanup** — CURRENT empty count = 0 |
| Delete hides a real requirement? | **Fail to falsify honesty** — text was empty; prior QA (`cyc-…125038Z`, `…130315Z`) already `authorized:false` for `dod:c70748809b8a` |
| Generator still ranks blanks? | **Fail to falsify filter** — unit test + harness skip `''`/`None`/`   ` |
| Silent `[x]` flip of other §25 items? | **Fail to falsify process** — only empty open line removed; L1402 `BLOCKED` semantics still open |
| NOT_APPLICABLE for campaign meta? | **Fail to falsify** — not used |
| Implementer self-QA only? | **Fail to falsify independence** — re-ran scans/tests here |
| parse_dod still accepts empties if reintroduced? | **Residual** — mitigated only at generate_candidates (low) |

---

## DoD action authorization (per item)

| Line | ID | Item text | Action |
|------|-----|-----------|--------|
| 1401 (former) | `dod:c70748809b8a` | *(empty orphan)* | **DELETE markup — do NOT [x] flip** |

**No DoD `[x]` flips authorized or performed.**

**Scope bar:** remove ranking-noise empty checkbox + harden candidate generator. Does **not** implement `BLOCKED` semantics or any other §25 claim language item.

---

## Issues (residual, non-blocking for PASS)

1. **LOW** — Empty-line defense only in `generate_dynamic_candidates`; optional harden at `parse_dod` / audit.  
2. **LOW** — Delivery still uncommitted on feature branch.  
3. **LOW** — Unit test asserts whitespace case; `None`/`''` covered by QA harness / `strip()` path.

---

## Allowed vs forbidden claims

**Allowed**
- Zero empty checkbox lines in current `DOD.md`.
- Generator skips blank item text (test-proven).
- Orphan was non-requirement markup; deletion is correct.
- No `[x]` flips in this slice; `BLOCKED` item remains open.

**Forbidden**
- Any substantive §25 requirement completed by this delete  
- `BLOCKED` / MetricStatus.BLOCKED done  
- VPS / 95% achieved / LOCAL_RESILIENCE_READY / PRE_VPS_FINAL_READY  
- Orphan marked DONE via `[x]`  
- Story Done without PO close  

---

## AC trace

1. **1 dod_item_id proven or left open** — PASS (empty markup deleted with evidence, not false-greened).  
2. **No NOT_APPLICABLE for meta** — PASS.  
3. **Independent QA before any `[x]` flip** — PASS (zero flips; this review is the independent gate).

---

## Next agents

1. **@po** — close story; **do not** invent an `[x]` for the deleted orphan; keep L1402 `BLOCKED` open until real proof.  
2. **@dev** — commit uncommitted empty-orphan work before publish.  
3. **@devops** — draft PR only after PO close + state reconciliation.

---

*Independent adversarial QA — implementer self-QA is not accepted as gate. No DoD `[x]` flips by this review.*
