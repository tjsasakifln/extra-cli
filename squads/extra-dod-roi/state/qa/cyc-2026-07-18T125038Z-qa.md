# QA Adversarial Review — cyc-2026-07-18T125038Z

**Story:** `ROI-cand-dyn-slice-cad02f320cc4`  
**Title:** [25] Indicator catalog — definição / fórmula / denominador / as_of / fonte / status / READY  
**Reviewer:** adversarial-qa-auditor (Quinn) — **independent** of delivery-engineer  
**Branch:** `extra-roi/cand-indicator-catalog`  
**Reviewed at:** 2026-07-18T12:55:20Z  

## Verdict: **PASS**

DoD flip authorized for **7 of 8** slice items.  
Empty orphan checkbox at `DOD.md:1401` must **remain open** (`authorized: false`).

---

## Commands re-run (independent)

| Command | Exit | Result |
|---------|------|--------|
| `python3 -m pytest tests/test_indicator_catalog.py -q -o addopts=` | 0 | **6 passed** |
| `validate_indicator_catalog()` + `export_indicator_catalog()` | 0 | `ok=true`, 6/6 defs, `issues=[]` |
| `pytest -k 'coverage_contract or indicator or metric_definition'` | 0 | **28 passed** |

Evidence pack `docs/ops/session-2026-07-18-indicator-catalog/` matches (pytest.exit=0, 6 passed, catalog-validation ok).

---

## Falsification results

| Attempt | Outcome |
|---------|---------|
| Empty / whitespace definitions accepted? | **Fail to falsify** — `required_fields_present()` rejects blanks |
| Missing catalog fields on any of 6 metrics? | **Fail to falsify** — all fields non-empty |
| READY stamped by code existence alone? | **Fail to falsify** — only `_ready()` sets READY; needs num/denom; semantics forbid it |
| Tests reimplement validation instead of calling shipped code? | **Fail to falsify** — import `validate_indicator_catalog` / `export_indicator_catalog` / helpers |
| Orphan empty line flipped? | **Fail to falsify** — still `[ ]` with empty text; implementer non-claim |
| `as_of` missing entirely? | **Fail to falsify** — `as_of_policy` on defs + report `as_of` (not on MetricResult — residual low) |

---

## DoD flip authorization (per item)

| Line | Item text | Flip? |
|------|-----------|-------|
| 1394 | Todo indicador possui definição. | **YES** |
| 1395 | Todo indicador possui fórmula. | **YES** |
| 1396 | Todo indicador possui denominador. | **YES** |
| 1397 | Todo indicador possui data de corte. | **YES** |
| 1398 | Todo indicador possui fonte. | **YES** |
| 1399 | Todo indicador possui status de prontidão. | **YES** |
| 1400 | `READY` significa executado e validado. | **YES** |
| 1401 | *(empty orphan)* | **NO — leave open** |

Scope of “todo indicador”: formal `ALL_METRIC_IDS` (6) in `scripts/coverage/coverage_contract.py`.

---

## Issues (residual, non-blocking)

1. **LOW** — READY semantics unit test is string-based; optional compute_* NOT_READY integration assert.  
2. **LOW** — `BLOCKED_SEMANTICS` exists without `MetricStatus.BLOCKED` (out of slice / L1403).  
3. **LOW** — Delivery still uncommitted on feature branch.

---

## Allowed vs forbidden claims

**Allowed**
- Catalog of 6 contract metrics has definition, formula, denominator policy, as_of policy, source policy.
- Results carry readiness status; READY means executed/validated (documented + structural).
- Empty orphan not claimed.

**Forbidden**
- All monorepo KPIs catalogued  
- 95% operational coverage / VPS / LOCAL_RESILIENCE_READY / PRE_VPS_FINAL_READY  
- Orphan line DONE  
- Story Done without PO close  

---

## Next agents

1. **@po** — flip the 7 authorized `DOD.md` checkboxes with evidence link to `docs/ops/session-2026-07-18-indicator-catalog/`; **do not** flip L1401; close story.  
2. **@dev** — commit uncommitted catalog work before publish.  
3. **@devops** — draft PR only after PO close + state reconciliation.

---

*Independent adversarial QA — implementer self-QA is not accepted as gate.*
