# Example output shape for *rank-next

See live run via:

```bash
python squads/extra-dod-roi/scripts/rank_next_cli.py --top 5
```

Expected sections: divergences, seals, top 5 ROI, selected for execute-next, blockers.
