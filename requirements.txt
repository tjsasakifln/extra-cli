# Core
httpx>=0.28.1
requests>=2.32.0
openai>=1.55.0
psycopg2>=2.9.9
# psycopg2-binary>=2.9.9  # Dev/test apenas — nao usar em producao
python-dotenv>=1.0.0
pyyaml>=6.0

# PDF Generation
reportlab>=4.5.1
pypdf>=6.15.0

# Excel
openpyxl>=3.1.5

# CLI
rich>=13.0.0

# EXTRA Command Center (local UI API)
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0

# Data processing
lxml>=5.0.0
beautifulsoup4>=4.12.0

# Fuzzy string matching (optional, fallback to difflib)
rapidfuzz>=3.0.0

# Optional: SICAF checking
# playwright>=1.40.0  # descomentar se precisar de SICAF

# Optional: Web automation for JS portals (FEAT-2.4)
# selenium>=4.15.0  # descomentar se precisar de crawlers com Selenium
# webdriver-manager>=4.0.0  # gerenciamento automatico de ChromeDriver
prometheus_client>=0.20.0

# Test / property-based (budget adversarial suite)
hypothesis>=6.100.0

# Predictive intelligence stack — canonical list: requirements-predictive.txt
# Kept here so CI single-file install remains complete; train path uses lazy imports.
-r requirements-predictive.txt
