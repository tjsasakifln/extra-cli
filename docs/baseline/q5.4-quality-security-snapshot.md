# Q5.4 — Quality / types / security / dependency snapshot

**Date:** 2026-07-16  
**Window:** C2 next-window  
**Scope:** lint + types + security static scan + dependency audit (snapshot only; **no remediations applied**)  
**Git push:** not performed (explicit constraint)

## Environment

| Tool | Version | Status |
|------|---------|--------|
| Python | 3.12.3 | available |
| ruff | 0.15.12 | available (`~/.local/bin/ruff`) |
| mypy | 1.20.2 | available |
| bandit | 1.9.4 | available |
| pip-audit | 2.10.1 | available |

## Commands executed

```bash
ruff check scripts/crawl/ scripts/golden_path.py
mypy scripts/crawl/ scripts/golden_path.py
bandit -r scripts/crawl -ll
pip-audit
```

---

## 1. Lint (ruff)

| Metric | Value |
|--------|-------|
| **Target** | `scripts/crawl/` + `scripts/golden_path.py` |
| **Errors** | **44** |
| **Auto-fixable (`--fix`)** | 30 (+ 8 hidden unsafe) |
| **Exit code** | 1 (non-zero; findings present) |
| **Honest status** | **FAIL as clean-lint gate** — residual debt, not a regression claim |

### By rule code

| Code | Count | Theme |
|------|------:|-------|
| F401 | 19 | unused-import |
| F841 | 8 | unused-variable |
| I001 | 4 | unsorted-imports |
| UP017 | 3 | datetime UTC style |
| UP035 | 3 | deprecated-import |
| F821 | 2 | undefined-name (correctness) |
| N818 | 2 | exception naming |
| S311 | 1 | non-crypto random (security-ish) |
| S608 | 1 | hardcoded SQL expression (security-ish) |
| UP041 | 1 | timeout error alias |
| **Total** | **44** | |

### Notable correctness hits

- `scripts/crawl/watermark.py` — `F821` undefined name `Any` (also mirrored by mypy `name-defined`)
- Mostly hygiene (unused imports/vars); ~68% auto-fixable without semantic change

### Critical-path note (Q5.1 contrast)

Q5.1 showed clean ruff on a **narrow** critical set (`monitor.py`, `golden_path.py`, `ciga_ckan_crawler.py`).  
This Q5.4 expands to **full** `scripts/crawl/` → 44 findings appear. Not contradictory: different surface area.

---

## 2. Types (mypy)

| Metric | Value |
|--------|-------|
| **Target** | `scripts/crawl/` + `scripts/golden_path.py` |
| **Source files checked** | 85 |
| **Files with errors** | 40 |
| **Errors** | **203** |
| **Exit code** | 1 |
| **Honest status** | **FAIL as strict type gate** — large pre-existing typing debt; config/import-path noise mixed with real type issues |

### Top error codes

| Code | Count | Notes |
|------|------:|-------|
| no-any-return | 51 | return type vs Any leakage |
| import-not-found | 36 | missing stubs / alternate import roots (`ingestion.*`, `supabase_client`, relative client paths) |
| no-untyped-def | 34 | missing annotations |
| arg-type | 18 | argument mismatches |
| attr-defined | 16 | missing/wrong attributes |
| assignment | 8 | |
| unreachable | 7 | |
| index / unused-ignore / union-attr / return-value / … | remainder | |

### Top files by error count

| File | Errors |
|------|-------:|
| `scripts/crawl/bids_crawler.py` | 14 |
| `scripts/crawl/_parallel_mixin.py` | 13 |
| `scripts/crawl/ciga_ckan_crawler.py` | 13 |
| `scripts/crawl/pncp_backfill.py` | 12 |
| `scripts/crawl/transparencia_crawler.py` | 12 |
| `scripts/crawl/clients/pncp/circuit_breaker.py` | 11 |
| `scripts/crawl/async_client.py` | 10 |
| `scripts/golden_path.py` | 9 |

**Interpretation:** a material share is **import resolution / stub / path layout** (`import-not-found`, `import-untyped`), not only pure logic type bugs. Treat as baseline debt for a dedicated typing wave, not a C2 blocker by itself unless product policy requires zero mypy.

---

## 3. Security static analysis (bandit)

| Metric | Value |
|--------|-------|
| **Target** | `scripts/crawl` recursive |
| **LOC scanned** | 23 793 |
| **Filter** | `-ll` (report severity ≥ MEDIUM) |
| **HIGH** | **0** |
| **MEDIUM (reported)** | **21** |
| **LOW (metrics totals, not listed under `-ll`)** | 22 (in full metrics) |
| **nosec suppressions** | 0 |
| **Exit code** | 1 |
| **Honest status** | **No HIGH findings.** MEDIUM residual around URL open + SQL string patterns; needs triage, not panic. |

### Reported MEDIUM by test id

| Test ID | Count | Theme |
|---------|------:|-------|
| B310 | 17 | `urllib.request.urlopen` / scheme audit (CWE-22 class) |
| B608 | 4 | possible SQL injection via string-built queries |
| **Total reported** | **21** | |

### Top files (reported issues)

| File | Issues |
|------|-------:|
| `doe_sc_crawler.py` | 5 |
| `mides_bigquery_crawler.py` | 3 |
| `ciga_ckan_crawler.py` | 2 |
| `dom_sc_crawler.py` | 2 |
| `transparencia_crawler.py` | 2 |
| others (1 each) | compras_gov, contracts, dlq, pcp, pncp_crawler_adapter, … |

**Caveat:** several `urlopen` call sites already carry `# noqa: S310` comments claiming hardcoded HTTPS endpoints — bandit still flags (no `# nosec`). Not automatically exploitable; still audit-worthy if any URL is user-controlled.

---

## 4. Dependency audit (pip-audit)

| Metric | Value |
|--------|-------|
| **Command** | `pip-audit` (environment-wide installed packages) |
| **Known vulnerabilities** | **135** |
| **Packages affected** | **38** |
| **Skipped (not on PyPI / distro packages)** | 10 (e.g. `python-apt`, Ubuntu meta packages, dfsg builds) |
| **Exit code** | non-zero (findings present) |
| **Honest status** | **Environment inventory is noisy.** Counts include system/dev/transitive packages, not a curated app lockfile-only audit. Use as **upper-bound signal**, not product SBOM verdict. |

### Highest volume packages (vuln rows)

| Package | Approx. vuln rows | Notes |
|---------|------------------:|-------|
| aiohttp | 38 | high volume; often transitive |
| pillow | 11 | |
| tornado | 10 | |
| starlette | 7 | |
| pip | 6 | tooling |
| gitpython / jinja2 | 5 each | |
| idna / setuptools / twisted | 4 each | |
| requests / urllib3 | 3 each | common HTTP stack |

### Project-relevant packages with hits (non-exhaustive)

`aiohttp`, `requests`, `urllib3`, `cryptography`, `pydantic-settings`, `python-dotenv`, `pyarrow`, `pytest`, `jinja2`, `pillow`, `starlette`, `tornado`, `twisted`, `pip`, `setuptools`, `black`, `filelock`, `h2`, `idna`.

**Recommendation for next wave:** re-run `pip-audit -r requirements.txt` (and/or lockfile) to separate **app runtime** risk from global site-packages noise.

---

## 5. Aggregate scorecard

| Gate | Result | Count / signal | Blocks C2 publish? |
|------|--------|----------------|--------------------|
| ruff (`crawl` + `golden_path`) | **FAIL clean** | 44 issues (30 auto-fixable) | Policy-dependent; not auto-blocker in this snapshot |
| mypy (same scope) | **FAIL clean** | 203 errors / 40 files | No — known debt; import-path noise |
| bandit `-ll` | **No HIGH** | 0 HIGH / 21 MEDIUM | No HIGH gate fail; MEDIUM triage backlog |
| pip-audit (env) | **Findings** | 135 vulns / 38 pkgs | Needs scoped re-audit before claiming secure deps |

### Overall honest status

**SNAPSHOT COMPLETE — not a green quality bill of health.**

- Static quality on full crawl tree is **yellow/red** (lint + types debt).  
- Security static scan has **zero HIGH** on crawl code; MEDIUM URL/SQL patterns remain.  
- Dependency surface is **red if interpreted as product risk**, but **overstates** until lockfile-scoped.  
- Q5.1 critical tests remaining green is orthogonal; this Q5.4 does **not** re-run pytest.

---

## 6. Suggested follow-ups (not done here)

1. **FAST/hygiene:** `ruff check --fix` for the 30 safe fixes; manually fix 2× F821/`Any` import.  
2. **Typing wave:** fix import roots / stubs first (cut ~36 `import-not-found`), then annotations.  
3. **Bandit triage:** classify B310 (fixed HTTPS vs user URL) and B608 (parameterized SQL).  
4. **Deps:** `pip-audit -r requirements.txt` (+ optional ignore list for non-runtime tools); prioritize `aiohttp`/`urllib3`/`requests` if in direct deps.  
5. Do **not** treat this file as authorization for push/release.

---

## 7. Reproducibility

```bash
ruff check scripts/crawl/ scripts/golden_path.py --statistics
mypy scripts/crawl/ scripts/golden_path.py
bandit -r scripts/crawl -ll
pip-audit
# preferred dep scope:
# pip-audit -r requirements.txt
```

**Artifact path:** `/mnt/d/extra consultoria/docs/baseline/q5.4-quality-security-snapshot.md`  
**Status:** DONE (snapshot only)
