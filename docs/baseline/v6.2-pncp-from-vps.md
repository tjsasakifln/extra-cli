# V6.2 — PNCP probe a partir da VPS Netcup (MNZ)

**Data:** 2026-07-23  
**Host:** `159.195.18.88` (RS 2000 G12 · Manassas)  
**SO:** Debian 13 (trixie)

## Resultado de rede (não é geo-block)

| Endpoint | HTTP | Latência (s) | Nota |
|----------|------|--------------|------|
| `GET /api/consulta/v1/contratacoes/publicacao?...&tamanhoPagina=1` | **400** | ~5.3 | Validação API (`tamanhoPagina` mínimo 10) — **não 403** |
| `GET /api/consulta/v1/contratacoes/proposta?...&tamanhoPagina=1` | **400** | ~23.4 | Idem validação |
| `GET https://pncp.gov.br/` | **302** | ~0.5 | Site responde |
| `GET https://dadosabertos.compras.gov.br/` | **302** | ~1.9 | OK |

**Conclusão parcial:** origem **MNZ (US)** alcança a API PNCP sem bloqueio geográfico 403. Ajustar `tamanhoPagina≥10` no smoke completo.

## Crawl app

- `prometheus_client` ausente do `requirements.txt` → instalado manualmente no venv da VPS.
- `monitor.py --source pncp --mode incremental` ainda precisa de smoke completo com page size correto e evidência de linhas gravadas (pendente).

## Próximo

```bash
ssh ec-prod
cd /opt/extra-consultoria && source .venv/bin/activate && source /root/.extra-pg-credentials
# page size ≥ 10 no adapter / flags do monitor
python scripts/crawl/monitor.py --source pncp --mode incremental --dsn "$LOCAL_DATALAKE_DSN"
```
