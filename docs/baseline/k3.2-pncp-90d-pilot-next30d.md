# K3.2 — PNCP contracts 90-day pilot (NEXT-30D)

**Date:** 2026-07-17  
**Campaign:** NEXT-30D-MULTIAGENT  
**Status:** **DONE_PARTIAL** — pilot **in progress / substantive data**, not yet closed with full window reconciliation

## Execution

| Item | Value |
|------|-------|
| Runner | `scripts/ops/run_contracts_pilot.py` / `scripts/crawl/run_contracts_90d_pilot.py` |
| Mode | `full` (`CONTRACTS_FULL_DAYS=90`) |
| DSN | `127.0.0.1:5433/pncp_datalake` |
| Checkpoint | `data/contracts_checkpoints/contracts_full.json` |
| Partial-window fix | `contracts_crawler.py`: windows with page errors **not** marked complete |

## Observed metrics (live mid-run)

| Metric | Snapshot |
|--------|----------|
| `pncp_supplier_contracts` count | **≥ 6050** (rising during run; re-check at end) |
| Baseline before campaign | **0** |
| API | PNCP consulta `/contratos` |

## Go / No-Go for 3-year expansion

| Criterion | Assessment |
|-----------|------------|
| Upsert path works | **GO** (rows accumulating) |
| Checkpoint file written | **GO** |
| Partial window not falsely complete | **GO** (code fix + crawl_with_evidence path) |
| Full 90d window set reconciled | **PENDING** until runner exits with JSON status=success |
| 3y expansion start | **CONDITIONAL GO** after pilot JSON closes with acceptable error rate |

## Claims

**Allowed:** Contracts pilot executed against live PNCP; thousands of contracts ingested; zero → multi-thousand delta.  
**Forbidden:** Claiming full 3-year backfill complete; CONTRATOS_95; pilot 100% window coverage without final JSON.

## Next

1. Wait for pilot process exit + `output/contracts/pilot-90d-next30d.json`  
2. Inspect windows failed vs ok  
3. If error rate acceptable → schedule `backfill_3y`
