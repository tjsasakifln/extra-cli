# C2.8 — DedupEngine wired (NEXT-30D)

**Date:** 2026-07-17  
**Campaign:** NEXT-30D-MULTIAGENT  
**Agent:** A6 (dedup)  
**Status:** **DONE** (CLI + multi-source proof + integration test)  
**Evidence report:** [`output/dedup/run-next30d.json`](../../output/dedup/run-next30d.json)

---

## 1. Claim

`DedupEngine` (`scripts/lib/dedup.py`) is **wired into an operational CLI**
(`scripts/crawl/run_dedup.py`), runs against live `opportunity_intel`, and
persists cross-source groups into `dedup_cross_source`.

Cross-source entity resolution is **proven with synthetic multi-source fixtures**
that share canonical fields (modalidade + objeto + órgão + data + valor) under
normalization. This is **not** production crawl coverage.

---

## 2. Before / after SQL counts

### 2.1 Historical baseline (pre-wiring, C2.8 status doc)

| Metric | Value | Source |
|--------|------:|--------|
| `dedup_cross_source` rows | **0** | `docs/baseline/c2.8-dedup-aliases-status.md` §3 |
| `dedup_groups` | **0** | same |
| `opportunity_intel` active | **5** | all `source=test_batch` |
| Multi-source present? | **No** | single source only |
| Production caller for DedupEngine | **None** | code-only |

### 2.2 Immediate before this A6 run (after prior wave fixtures)

Captured in `output/dedup/before-sql-counts.json` / `run-next30d.json.before`
(environment already had 1 synthetic triple from earlier NEXT-30D work):

| Metric | Value |
|--------|------:|
| `dedup_cross_source` rows | 3 |
| `dedup_groups` | 1 |
| `opportunity_intel` active | 8–10 |
| sources | `test_batch`, `pncp`, `pcp`, `sc_compras` (+ later `transparencia_sc`) |

### 2.3 After A6 synthetic pair + re-run

**Command:**

```bash
export DATABASE_URL=postgresql://test:test@127.0.0.1:5433/pncp_datalake
export PYTHONPATH=.
# seed synthetic pair (pncp + transparencia_sc, same canonical hash)
PYTHONPATH=. python3 scripts/crawl/run_dedup.py \
  --seed-synthetic \
  --output output/dedup/run-next30d.json
```

**Actual run report** (`output/dedup/run-next30d.json`):

| Metric | Before | After | Δ |
|--------|-------:|------:|--:|
| `dedup_cross_source` rows | 3 | **5** | **+2** |
| `dedup_groups` | 1 | **2** | **+1** |
| `opportunity_intel` active | 10 | 10 | 0 |
| `engine_stats.groups_found` | — | **2** | — |
| `engine_stats.duplicates` | — | **5** | — |
| `engine_stats.inserted` | — | **5** | upsert |

**SQL equivalent (after):**

```sql
SELECT count(*) AS dedup_rows FROM dedup_cross_source;
-- 5

SELECT count(DISTINCT dedup_group_id) AS dedup_groups FROM dedup_cross_source;
-- 2

SELECT source, count(*) FROM opportunity_intel
WHERE is_active IS TRUE GROUP BY source ORDER BY 2 DESC;
-- test_batch:5 | pncp:2 | pcp:1 | sc_compras:1 | transparencia_sc:1
```

### 2.4 Group detail (after)

| Group | Sources | Members | Canonical | Notes |
|-------|---------|--------:|-----------|-------|
| G1 | pncp, pcp, sc_compras | 3 | `next30d-pncp-fixture-1` | prior NEXT-30D synthetic triple |
| G2 | pncp, transparencia_sc | 2 | `SYNTH-DEDUP-NEXT30D-PNCP-001` | A6 synthetic pair (normalization proof) |

Full dump: `output/dedup/after-sql-counts.json`.

---

## 3. Synthetic fixtures (honest labelling)

**These are synthetic proof rows, NOT production coverage.**

| Field | Value |
|-------|-------|
| Prefix `source_id` | `SYNTH-DEDUP-NEXT30D-*` (A6) / `next30d-*-fixture-1` (prior wave) |
| Prefix `content_hash` | `synth-dedup-next30d-*` |
| metadata | `{"synthetic": true, "not_production": true, "purpose": "NEXT-30D C2.8 ..."}` |
| Canonical shared | orgao_cnpj `82892324`, data `2026-07-01`, valor `150000`, objeto material de escritório |
| Normalization proof | `Pregão Eletrônico` vs `pregao eletronico` + accent-stripped objeto → same SHA-256 |

Seed helper (idempotent cleanup+insert):

```python
from scripts.crawl.run_dedup import seed_synthetic_fixtures
seed_synthetic_fixtures(conn)
```

Or CLI: `--seed-synthetic`.

---

## 4. Artifacts

| Path | Role |
|------|------|
| `scripts/crawl/run_dedup.py` | Operational CLI + `--seed-synthetic` + before/after JSON |
| `scripts/lib/dedup.py` | DedupEngine (CM-13) |
| `tests/test_dedup_engine_pipeline.py` | Integration test (skip if no DB) |
| `output/dedup/baseline-before.json` | First baseline (single-source / partial) |
| `output/dedup/run-next30d.json` | A6 proof run (groups_found=2, rows=5) |
| `output/dedup/after-sql-counts.json` | SQL group detail |
| `docs/baseline/c2.8-dedup-aliases-status.md` | Pre-wiring status (PARTIAL) |

---

## 5. Tests

```bash
REQUIRE_TEST_DB=1 \
DATABASE_URL=postgresql://test:test@127.0.0.1:5433/pncp_datalake \
PYTHONPATH=. pytest tests/test_dedup_engine_pipeline.py -v --no-cov
```

Coverage:

1. **dry-run** with multi-source fixtures → `groups_found >= 1`, no writes  
2. **persist** → 2 rows / 1 group / 2 sources in `dedup_cross_source`  
3. **seed_synthetic_fixtures** helper → dry-run finds groups  
4. **unit fallback** (no DB) → hash stability for accent/case variants  

---

## 6. Honest claims

### Allowed

- DedupEngine is callable via production-shaped CLI and writes `dedup_cross_source`.
- Cross-source groups form when ≥2 sources share a canonical hash (normalization-aware).
- Integration test proves `groups_found > 0` against a live DB (skip if unavailable).
- `dedup_cross_source` count after proof: **5** rows / **2** groups.

### Forbidden

- Claiming production multi-portal coverage improvement from synthetic fixtures.
- Marking DoD §6.2 fully accepted without golden-dataset recall.
- Claiming C2.8 “aliases + entity matching + production reconciliation” closed end-to-end
  (aliases seed remains separate; see `c2.8-dedup-aliases-status.md`).

---

## 7. Residual gaps (out of A6 scope)

1. Real multi-source crawl data still thin (fixtures dominate proof).  
2. No scheduled job / post-crawl hook yet — CLI is the wiring, not a timer.  
3. Two dedup motors remain (`lib/dedup.py` vs `opportunity_intel/dedup.py` ingest).  
4. Golden dataset re-validation still open (plan evidence note).
