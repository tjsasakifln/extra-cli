# C2.8 — DedupEngine wired (NEXT-30D)

**Date:** 2026-07-17  
**Campaign:** NEXT-30D-MULTIAGENT  
**Status:** **DONE** (pipeline CLI + operational proof with multi-source fixtures)

## Before / after

| Metric | Before | After |
|--------|--------|-------|
| `dedup_cross_source` rows | 0 | **3** |
| `dedup_groups` | 0 | **1** |
| `opportunity_intel` active | 5 (single source `test_batch`) | 8 (pncp+pcp+sc_compras fixtures + test_batch) |
| CLI | none | `scripts/crawl/run_dedup.py` |

## Commands

```bash
export DATABASE_URL=postgresql://test:test@127.0.0.1:5433/pncp_datalake
PYTHONPATH=. python3 scripts/crawl/run_dedup.py --output output/dedup/baseline-before.json
# seed multi-source fixtures (synthetic NEXT-30D)
PYTHONPATH=. python3 scripts/crawl/run_dedup.py --output output/dedup/run-next30d-after-fixture.json
```

## Engine stats (after fixtures)

```json
{
  "total_ops": 8,
  "groups_found": 1,
  "duplicates": 3,
  "inserted": 3,
  "outcome": "success"
}
```

## Honest claims

**Allowed:** DedupEngine is callable via CLI; writes `dedup_cross_source`; detects cross-source groups when multi-source rows share canonical hash.

**Forbidden:** Claiming production coverage improvement from synthetic fixtures alone; claiming DoD §6.2 fully accepted without golden dataset recall.

## Fixtures

Synthetic rows `source_id LIKE 'next30d-%'` with shared modalidade/objeto/CNPJ/data/valor across `pncp`, `pcp`, `sc_compras`. Marked in `proveniencia.fixture=NEXT-30D-MULTIAGENT`.
