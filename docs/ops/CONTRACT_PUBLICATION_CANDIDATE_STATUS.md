# CONTRACT_PUBLICATION_CANDIDATE_STATUS

Family: `contract-publication-candidate/1.0` + `contract-evidence-pack/1.0`
Issue: extra-cli#414
As of: 2026-08-16

The producer in `scripts/contract_publication/` is deterministic and
fail-closed. The golden corpus is a labeled fixture. The `--live` command
emits `OFFICIAL_DATA_UNAVAILABLE` in the same schema.

This is not permission to index and not a claim that official live ranking
was executed.
