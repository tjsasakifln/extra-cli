# Bid submission readiness

**Status: DRAFT / BLOCKED** — operational proof requires authorized **public real**
bid document corpus (issue #137). Golden fixtures under `scripts/bid_readiness/fixtures/`
are synthetic only and must not be claimed as READY_TO_SUBMIT evidence.

```bash
python -m scripts.bid_readiness --help
```
