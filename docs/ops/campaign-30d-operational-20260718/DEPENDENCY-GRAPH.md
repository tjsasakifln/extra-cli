# DEPENDENCY GRAPH (session)

```
recovery publish → continue epic
  → ranking[0] b8d41f (estados/aplicabilidade)
    → re-QA → PO → merge
      → force-next
        → next ranking[0] (likely coverage / operational P0)
VPS ─┬─ BLOCKED_EXTERNAL (parallel local work continues)
coverage 95% ─┴─ long local path
```
