# ERRATA — Legacy claim 214 / 1.093 = 19,5791%

**Campaign:** `DUAL-CAPABILITY-COVERAGE-TRUTH-01`  
**Date:** 2026-07-21  
**Status:** SUPERSEDED / NON-CANONICAL  

## Prior evidence (preserved historically)

| Field | Value |
|-------|-------|
| DOD item | §12.1 “O golden path calcula cobertura” |
| Evidence pack | `.dod/evidence/DOD-rol-1-definition-of-done-4efe05fc94/` |
| PR | #83 |
| Reported | den=1093 · num=214 · pct=19.5791 |
| Method | `entity_coverage.is_covered` (with `any_row` fallback in code) |

This pack remains on disk for audit lineage. **Do not delete.**

## Why it is semantically invalid as canonical coverage

1. **No capability split** — does not compute `open_tenders` and `historical_contracts` separately.
2. **No set equality** of numerator IDs to applicable canonical universe (join/audit missing).
3. **`is_covered` semantics** ≈ “publicações recentes” admin flag, not seven-stage monitoring coverage.
4. **`any_row` fallback** treated mere administrative row presence as coverage.
5. **No freshness participation** in the numerator under dual SLAs.
6. **No validated `success_zero` gate**.
7. **Cannot support dual 95% gates** without compensation/averaging risk.

Therefore **19,5791% must not be cited as**:

* cobertura geral;
* cobertura canônica de editais;
* cobertura de contratos;
* proof of DOD dual 95%.

## Replacement definition

See:

* `specs/001-dual-capability-coverage-truth/spec.md`
* `docs/architecture/adr/ADR-030-dual-capability-coverage-truth.md`
* `scripts/coverage/dual_capability_coverage.py`

## Replacement measurement

```bash
export LOCAL_DATALAKE_DSN=...
python3 -m scripts.coverage.dual_capability_coverage --capability both --output-dir output/coverage
# or
python3 -m scripts.golden_path --execute-dual-coverage-only --capability both
```

Outputs:

* `output/coverage/dual-capability-coverage-summary.json`
* `output/coverage/dual-coverage-{open_tenders,historical_contracts}.json`
* `output/coverage/dual-coverage-gaps-*.csv`

## Supersession rule

| Claim | Status |
|-------|--------|
| 214/1093=19.5791% as golden-path coverage | **SUPERSEDED** |
| Dual capability_monitoring_coverage | **CANONICAL** (measurement) |
| Live ≥95% either capability | **NOT CLAIMED** until dual-definition live proof |

New acceptance packs MUST reference dual artifacts + method=`dual_capability_coverage`.
They MUST list `claims_forbidden` including the legacy percentage as general coverage.
