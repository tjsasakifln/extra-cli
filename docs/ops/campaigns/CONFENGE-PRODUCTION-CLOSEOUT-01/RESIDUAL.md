# Residual — extra-cli

- `#302` persist tables absent on `pncp_datalake`. National claim remains unauthorized.
- `#415` cannot emit `official_live=true` until official semantic columns exist. Do not relax.
- `#414` evidence pack official_live not produced this campaign (publication snapshot path not materialized on host extra SHA `bbc4b6b7`).
- `#400` consumer live for Market Answer SC only. Analysis + X-Ray still `NEEDS_DATA`.
- Incremental unit failed today on source population drift. Do not reopen historical backfill. Next timer Wed 2026-08-19.
- VPS extra-cli checkout is `bbc4b6b7`, behind `origin/main` `2d68272d`. Producers ran from laptop worktree against live DSN.
- `public_read_v1` is empty. Do not invent X-Ray from `pncp_supplier_contracts` share-without-denominator.
- Issues stay OPEN except if a later DoD is fully met. This campaign does not close `#302/#400/#414/#415`.
