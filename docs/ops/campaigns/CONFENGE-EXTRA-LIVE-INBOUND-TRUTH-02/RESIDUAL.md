# CONFENGE-EXTRA-LIVE-INBOUND-TRUTH-02 — residual

- Host `pncp-contracts.service` still runs the **pre-convergence** incremental. Until this branch is deployed, the 06:00 unit will keep failing small monotonic growth as `source_population_drift`.
- `INCREMENTAL_HEALTHY` is **not** declared. The 2026-08-17 unit ended `failed` after 261 inserts.
- No EDITORIAL_REVIEW pack: official SC rows lack structured amendments/documents. State is `HOLD_FOR_DATA` (`stale_evidence` / missing documentary facts) + `NOT_COMPARABLE`.
- #415 remains open. Official columns `unidade`, `quantidade`, `regime`, `modalidade`, `valor_semantic` are still absent. Do not invent them.
- #400 / #414 remain open. One HOLD sample is not the full publication DoD.
- #302 remains the national gate. `public_read_v1` relation is absent on the host. No X-Ray fill.
- Live laptop DSN is absent; SELECTs ran over SSH. Incremental **write** against the host lake was not repeated (writer already inserted today; backfill was not restarted).
- OCR was not run.
