# public-read-bofu-evidence/1.0

Pack JSON is generated, not stored in Git.

```bash
python3 -m scripts.bofu_evidence --out DIR --snapshot SNAPSHOT.json \
  --national-coverage COVERAGE.json --comparable COMPARABLE.json --as-of ISO
```

`publication`, `index` and `national` remain false. READY is producer-ready, not page authority.
