# public-read-bid-readiness/1.0 export

Fully fictional/redacted consumer fixture. No real bid documents.

- `schema.json` — envelope schema
- `fixture.public.json` — redacted envelope (`source_access=redacted_fixture`)
- `web-cfg-155-read-model.json` / `.sql` — SELECT-only read model
- `manifest.json` — `publication_authorization=false`, `index_authorization=false`, `page_authorized=false`

Future consumer: web-cfg #155 (not closed here). Not a live page and not an index grant.
