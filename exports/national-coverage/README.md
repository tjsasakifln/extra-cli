# exports/national-coverage

Small, policy-compliant CLI outputs for the national coverage contract.

Do not commit bulk dumps, raw official payloads, or secrets. Regenerable JSON
from fixtures belongs in `docs/contracts/national-coverage/fixtures/`.
