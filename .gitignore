

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
__pycache__/

# Runtime outputs (Extra Consultoria)
output/excels/
output/golden-path/
output/bootstrap/
output/readiness/
output/runs/
output/resilience/

# Agent memories (runtime)
.claude/agent-memory/
*.pyc

# Service Account Keys (sensitive)
config/*-sa.json
*service-account*.json
*sa-key*.json

# Playwright MCP artifacts
.playwright-mcp/

# AIOX agent memory & pipeline runtime
.aiox/pipeline/
plan/

# QA gate artifacts (runtime)
docs/qa/gates/

# Profile calibration & quality scan outputs (runtime)
output/profile-calibration/
output/quality/

# Analysis docs (runtime)
docs/analysis/

# Coverage reports (runtime)
.coverage
htmlcov/
output/reports/

# Pipeline runtime
pipeline/*.json
pipeline/*.log
!pipeline/.gitkeep

# Data artifacts (runtime)
data/raw/
data/edital_workspace/
data/proposal_workspace/
data/pncp_sc_checkpoints/
data/*_checkpoints/*.json
data/cnpj_cache.json
data/geocode_cache.json
data/scrape_residual_progress.json
data/extra_ledger.json
db/log/

# Reproducible operational runs. Small summary/manifests are versioned only
# when a story explicitly promotes them as evidence.
output/ciga_dom/*/
output/doe_sc/*/
output/pncp_sc/*/
output/sc_compras/*/
output/sc_compras/*.jsonl
output/reconciliation/*/
output/coverage/acquisition/
output/session-evidence/*
scripts/output/

# Environment files — keep only .env.example tracked
.env.dev
.env.production
.env.staging

# Local crawler checkpoints (runtime)
data/contracts_checkpoints/
output/session-*/

# Campaign hygiene — reproducible/local artifacts (EXTRA-OPS-95+)
*.dump
*.sql.dump
**/*.pre-purge*
**/*.pre-purge-mismatch
data/cnpj14_cache/*.bak
data/cnpj14_cache/*.pre-purge*
output/qw-01/**/universe_snapshot.json
output/qw-01/**/radar_editais.xlsx
output/qw-01/**/coverage_gaps.csv
docs/ops/campaigns/**/evidence/**/*.dump
docs/ops/campaigns/**/evidence/**/package-final-fixture/*.pdf
docs/ops/campaigns/**/evidence/**/package-final-fixture/*.xlsx

# Spec Kit / Grok runtime (keep skills + constitution + workflows definitions)
.specify/workflows/**/runs/
.specify/.cache/
.grok/tmp/
.grok/cache/
specs/**/checklists/.runtime/
# DOD controller runtime logs are versioned lightly; large ephemeral packs optional
.dod/evidence/.tmp/

# Grok agent runtime (version skills only via negation if parent ignored)
.grok/*
!.grok/skills/
!.grok/skills/**
